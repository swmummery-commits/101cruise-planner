# Route-map PNG fonts

Noto Sans (SIL Open Font License 1.1) used only when rasterising route-map SVGs to PNG via resvg.
Browsers still render the SVG with the theme font stack; these files are for serverless PNG conversion where system fonts are unavailable.
